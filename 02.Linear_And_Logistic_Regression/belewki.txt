1.Защо правим Data Cleaning - за да разберем повече от/за данните, с които работим.
Базови методи за работа с ML и Scikit Learn.

ML не е само fit and predict.
Как може да започнем с базови модели за моделиране на данни. 
Базови модели за регресия и квалификация.

Линейните модели са прости и много бързи за пускане.
В ML основния принцип, който трябва да ни води, е бръснач на Оккам - избиране на подход към нещо.
Occam's razor
При 2 подхода - този от тях, който е по-прост, или имаме по-малко предположения.
По прост в ML означава по-малко параметри.
По-малко предположение - по-малко хипотези, по-малко bias в данните.

То при прост линеен модел и непрост нелинеен модел - избираме простия.
И почваме с него - другите трябва да надскочат.
Трябва да можем да итерираме бързо.
Но отначало трябва да имаме базова оценка, които се дават от доста прости, но доста силни модели.
С тяхна помощ ще правим надграждания 
- например ако искаме да направим полиномиална регресия
- какво става ако имаме аутлайъри
- какво става ако подавам данните неправилно.

Какво правим с линейната регресия:
използваме я, когато може да предположим, че входните данни имат линейна връзка с изходните.
Какво са входни данни - таблица nxm - n реда и m атрибута, feature или както ги наречем.
Всяка една колона представлява е едно  измерение в данните. - бидейки нещо което измерваме.
Един запис от своя страна представлява една точка в m-мерното пространство.
Разглеждаме dataset-а като едно векторно пространство.
Всеки един запис ще бъде с някакви координати m-мерното пространство.
Отделните записи се приемат като точки в пространство.
Тези точки трябва да ги моделираме - какво означава да ги моделираме - да разберем при входни данни m на брой колко е изходната променлива при определена хипотеза. В случая хипотезата е, че изходната променлива е линейна комбинация на входните променливи при коефициенти, които са неизвестни:
𝑦 = 𝑎1𝑥1 + 𝑎2𝑥2 + ⋯ + 𝑎𝑛𝑥𝑛 + 𝑏

Защо А транспонирано по х - защото данните ни идват във форма на стълбове.
Неизвестните коефициенти трябва в някакъв момент да ни станат известни. Само при линейна регресия може да ги намерим.

Ако имаме 2 хипотеза за данните:
1 - черната линия - h1.
2 - някаква оранжева - h2.

Трябва да намерим метрики за подобие.
Метриките за разстояние имат огромно значение. Има много от тях. В зависимост от това каква избираме даваме различна задача на модела.
Cost function.
Затова в scikit learn имаме еди какво си Classifier и Regressor.
Това в линейните модели го няма, защото Regressor e регресия, Logistic Regression винаги е класификация. 

Това е ограничение, зашото в scikit learn може да се прави класификация на 1 изходна променлива, и регресия на 1 изходна променлива.
Не може да се правят много неща наведнъж.

Освен това не могат да се решават задачи в scikit learn, които имат по-специален изход.
Пример autocompletion. Тях можем да ги докараме до класификация, ама после нямаме задачи за класификация.
Или най-простото - търсене на обекти по изображение - нямаме никакъв вид генериране на данни - не е  регресия, нямаме класификация, нямаме клъстериране дори - само семплиране.

Така наречената total cost function - за всичките данни. За 1 запис се нарича loss function. 
Тя е една от много.
Тя определя какво правим. 
Нещото което казва кой модел е по-добър - като сметнем J за черната линия и J за оранжевата линия, да кажем black e по-малко от orange. И това е някаква мярка за разстояние, която в случая се нарича средна квадратична грешка (Mean Squared Error).
!!!! Има и много друго, но J има голямо значение - то определеля каква задача решаваме.
loss функцията определя какво минимизираме, определя колко близко модела е до реалността.

Имаме performance metric - това е нашата метрика за това колко добре се справя нашия модел. 
И тогава тренирането представлява на база на тази метрика модела да се представя по-добре с времето.

Самият минимум колко е не ни интересува.
Само искаме на всяка итерация е да намалява разстоянието. 
Искаме аргументите a, b, при които това се случва. Не делим b, затова наричаме а вектор.
И може да имаме много измерения - всички начални аргументи, при които се минимизира. Това важи за всяка една ML задача.

И след това има алгоритъм, който минимизира тая loss функция. Написали сме я, и трябва да я минимизираме по някакъв начин.
Този алгоритъм се нарича Solver или Optimizer (TensorFlow).
Ако един Solver не работи, го сменяме.

Данните не се променят - x, y. Константи са, така че градиентите спрямо x, y = 0. Затова ни интересуват движенията по a, b. И затова използваме gradient descent  - градиента на J e вектор от всички подредени частни производни. Тъй като можем да имаме колкото параметъра имаме. 
Един параметър на модела е едно измерение, което оптимизираме с gradient descent. Ако имаме m на брой входни променливи и правим линейна регресия за тях, имаме m + 1 параметъра - по 1 за всяка една колонка + 1 bias. Ако имаме по-сложен модел, който не е линейна регресия, имаме повече на брой параметри. В невронна мрежа може да са милиарди. 

Не ни интересува стойността на loss фунцкията , интересуват ни координатите на тази точка, при който се постига минимум.
При loss функция ако сме намерили 1 локален, това е и глобалния. 

Но на друго място - може да имаме навсякъде 0 градиенти - тоест екстремум - ама да имаме saddle point. Вторите производни не са 0.
При едно измерение saddle point се нарича инфлексна точка - inflection point. 



 







 



